import React, { createContext, useState, ReactNode, useEffect } from 'react';

// Définir le type du contexte
const GlobalContextType:any = {
  cookie_base : 0,
  add_on_click : 1,
  add_on_time : 1
};

export default GlobalContextType;